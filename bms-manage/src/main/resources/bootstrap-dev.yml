spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8001
      config:
        enabled: true
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
        group: BMS_GROUP
        prefix: bms-manage